{
  "name": "npm_aprendendo",
  "version": "1.0.0",
  "description": "aprendnedo npm",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start:dev":"node --watch src/index.js",
    "start:dev:env":"node --env-file=.env --watch src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "iniciar": "echo \"Rodando via script\" && node src/index.js"
    
  },
  "author": "paulo h",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "log-symbols": "^7.0.0"
  }
}
